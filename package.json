{
  "name": "postwind",
  "version": "0.5.0",
  "description": "Lightweight Tailwind CSS v4 extension — pipe notation, shortcuts, visible: animations, and more",
  "type": "module",
  "main": "dist/postwind.js",
  "exports": {
    ".": {
      "import": "./dist/postwind.js",
      "require": "./dist/postwind.cjs"
    },
    "./lib": {
      "import": "./dist/lib.js",
      "require": "./dist/lib.cjs"
    },
    "./package.json": "./package.json"
  },
  "files": [
    "dist/",
    "src/",
    "example/",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist/ && bun build src/bundle.js --outfile dist/postwind.js --format esm && bun build src/bundle.js --outfile dist/postwind.min.js --format esm --minify && bun build src/bundle.js --outfile dist/postwind.cjs --format cjs && bun build src/lib.js --outfile dist/lib.js --format esm && bun build src/lib.js --outfile dist/lib.min.js --format esm --minify && bun build src/lib.js --outfile dist/lib.cjs --format cjs",
    "start": "bun bin/server.js",
    "test": "bun test src/",
    "dev": "bun bin/server.js & bun build src/lib.js --outfile dist/lib.js --format esm --watch & bun build src/bundle.js --outfile dist/postwind.js --format esm --watch",
    "pages": "open https://dux.github.io/postwind/",
    "open": "open https://www.npmjs.com/package/postwind",
    "gh": "open https://github.com/dux/postwind",
    "publish": "npm version patch --no-git-tag-version && bun run build && bun run test && npm publish"
  },
  "keywords": [
    "css",
    "tailwind",
    "tailwindcss",
    "utility-first",
    "responsive",
    "frontend",
    "bun",
    "pipe-notation",
    "shortcuts"
  ],
  "peerDependencies": {
    "@tailwindcss/browser": "^4.0.0"
  },
  "peerDependenciesMeta": {
    "@tailwindcss/browser": {
      "optional": false
    }
  },
  "author": "Dux",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/dux/postwind.git"
  },
  "homepage": "https://dux.github.io/postwind/",
  "bugs": {
    "url": "https://github.com/dux/postwind/issues"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
